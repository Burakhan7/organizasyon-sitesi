@model EtkinlikFormViewModel

@{
    ViewData["Title"] = "Etkinlik Düzenle";
}

<div class="container-fluid px-4 py-4">
    <h1 class="mb-4">Etkinlik Düzenle</h1>
    <div class="col-lg-8">
        <form asp-action="Duzenle" method="post" enctype="multipart/form-data" class="iletisim-form">
            <partial name="_EtkinlikForm" model="Model" />
            <button type="submit" class="btn btn-primary">Güncelle</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Vazgeç</a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}